//! Compile-closure stub for `subc-core` (private 0.3.1). The inventory
//! records exactly one row for this crate: a declared-only dev-dependency
//! edge with no `use subc_core` anywhere in the repository (the real
//! requirement is a spawnable daemon BINARY, owned downstream by mc-host).
//! An empty library is therefore the strict stub.
