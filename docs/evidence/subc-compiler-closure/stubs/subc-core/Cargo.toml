[package]
name = "subc-core"
version = "0.3.1"
edition = "2021"
publish = false
description = "STRICT compile-closure stub. subc-core is a declared-only dev-dependency edge (no `use subc_core` anywhere); the stub is deliberately empty. Never run."

# Cargo must treat this directory as a workspace root to avoid joining the enclosing workspace.
[workspace]
