[package]
name = "subc-client-rs"
version = "0.3.1"
edition = "2021"
publish = false
description = "STRICT compile-closure stub. Surface = verified subc API inventory rows only. Never run."

[dependencies]
async-trait = "0.1"
serde_json = "1"
subc-protocol = { path = "../subc-protocol" }
