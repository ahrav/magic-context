# The preload redirects cortexkit's shared database to a temporary directory so CLI tests cannot read or migrate the user's database.
# The CLI imports plugin storage through @magic-context/core.
# A bare openDatabase() call would otherwise resolve the live database path.
# resolveDatabasePath() also redirects when NODE_ENV=test.
[test]
preload = ["../plugin/test-preload.ts"]
