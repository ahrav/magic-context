# The test preload sets XDG_DATA_HOME to a temporary directory for the test process.
# The preload prevents tests from reading or migrating the user's CortexKit database shared with OpenCode.
# The test preload must remain configured to prevent migrations of the live database.
[test]
preload = ["./test-preload.ts"]
